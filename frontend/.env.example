# ── Frontend Environment Variables ───────────────────────────────────────────
# Copy this to .env (for local dev) or .env.production (for builds)

# Backend API base URL — the Vite dev server proxies /api → localhost:8000
# For production builds point this to your real backend URL
VITE_API_URL=/api
